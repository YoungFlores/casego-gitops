apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  namespace: casego-apps
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - casego.ddns.net
      secretName: frontend-tls
  rules:
    - host: casego.ddns.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
