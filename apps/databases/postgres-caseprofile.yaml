---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-caseprofile
  namespace: casego-apps
  labels:
    app: postgres-caseprofile
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: postgres-caseprofile
  template:
    metadata:
      labels:
        app: postgres-caseprofile
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: POSTGRES_USER
              value: caseprofile_user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: casego-db-credentials
                  key: POSTGRES_CASEPROFILE_PASSWORD
            - name: POSTGRES_DB
              value: caseprofile_db
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 256Mi
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "caseprofile_user", "-d", "caseprofile_db"]
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-caseprofile-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-caseprofile-data
  namespace: casego-apps
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-caseprofile
  namespace: casego-apps
spec:
  selector:
    app: postgres-caseprofile
  ports:
    - port: 5432
      targetPort: 5432
